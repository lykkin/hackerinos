:plain
    $(document).ready(function(){
        $("##{@hackerino.id}").children('.donger').replaceWith(
            '#{link_to image_tag("lowered.png"), '/vote/' << @hackerino.id.to_s, remote: true, method: :post, class:'donger'}'
        );
    });
